var React = require('react');

var Footer = React.createClass({

  render: function () {
    var allList = this.props.allTodos;
    return(
      <div>Footer</div>
    );
  }

});

module.exports = Footer;